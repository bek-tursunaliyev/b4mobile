import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        // Keep rarely-changing vendor code in its own chunk so the
        // Telegram WebView can cache it across deploys — most updates
        // only touch app code, so returning users skip re-downloading
        // React/Supabase/etc. on every release.
        manualChunks: {
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
          supabase: ['@supabase/supabase-js'],
        },
      },
    },
  },
})
